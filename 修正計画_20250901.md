# 修正計画書 - 2025年9月1日

## お客様からの修正依頼一覧

### 報告された問題（12項目）

1. **枚数多くアップロードするとエラーが頻発する**
2. **特定の画像がプリセットできない原因特定**（画像LINEで送ってる）
3. **画像2枚アップロードして、忘れた画像あったので、もう1枚画像をアップロードした場合、追加分がエクセルにでてこない**
4. **現在回転数の文字のところに通常回転数が入ってる。通常回転数が消えてる**
5. **出玉詳細は読み込めるが、エクセルと回転率②のところに反映されない**
   - グラフと出玉詳細で同じ台番の画像がある時は出玉詳細の数字を優先する
   - 出玉詳細が無い時はグラフ優先
6. **総獲得玉数の計算問題**
   - 出玉詳細の画像とグラフの画像の同じ台番がある時に、総獲得玉数を予め決めた固定の数字と(大中小)と現在地にて計算する
   - それがエクセルと回転率②のところに反映されてない
7. **3台アップロードして、違う台取りたいのでファイル削除。再度10台アップロードして解析すると、3台分しかエクセルに表示がされない**
8. **同じ台番が無い出玉詳細は表示しないようにする**
   - グラフしか無いのは通常通りグラフのみで計算
9. **エクセルのところ並び変える機能があると良い**
   - 貼り付けた順？台番がバラバラになるので台番順、回転率①順、回転率②順の3つ選択出来るようにしたい
10. **台番が読み込めない画像があるので、原因究明**（対応してもらってるのかも?）
11. **現在値と初当たり玉数の精度を寄せたい**（対応してもらってるのかも？）
12. **大、中、小の部分はエラーが出てるのでまだテストして無い**

## 重要な計算式の修正

### 総獲得玉数の正しい計算方法
```
総獲得玉数 = 総払い出し球数 - 現在値
（グラフが+なら引き算、グラフが-なら足し算）
```

### 回転率の計算
- **回転率①**: 初当たり回転数 ÷ (使用球数 ÷ 250)
- **回転率②**: 通常回転数 ÷ (使用球数 ÷ 250)

## 修正計画

### 🔴 優先度高 - すぐ修正

#### 1. 総獲得玉数の計算修正（問題5,6）
**現在の実装:**
```python
総獲得玉数 = 初当たり玉数 + 総払い出し球数
```

**修正後:**
```python
# AIから取得した総払い出し球数がある場合
if 総払い出し球数（AI）:
    if 現在値 >= 0:
        総獲得玉数 = 総払い出し球数 - 現在値
    else:
        総獲得玉数 = 総払い出し球数 + abs(現在値)
```

#### 2. 回転率②の計算修正
**修正内容:**
- 正しい総獲得玉数を使用して再計算
- 出玉詳細優先ロジックの実装

#### 3. 現在回転数と通常回転数の表示問題（問題4）
**調査箇所:**
- Claude APIの`current_rotations`と`normal_rotations`のマッピング
- データフレーム列への値の割り当て

#### 4. エクセル表示のリセット問題（問題3,7）
**修正内容:**
- セッションステートの管理改善
- ファイル削除時の処理
  ```python
  # ファイル削除時
  if 'edited_df' in st.session_state:
      del st.session_state['edited_df']
  if 'temp_df' in st.session_state:
      del st.session_state['temp_df']
  ```

### 🟡 優先度中 - 次に対応

#### 5. 同じ台番なし出玉詳細の非表示（問題8）
**実装内容:**
```python
# ペアリングされていない出玉詳細を除外
if not detail.get('paired'):
    continue  # 表示しない
```

#### 6. エクセルの並び替え機能（問題9）
**実装内容:**
```python
sort_option = st.selectbox(
    "並び順",
    ["アップロード順", "台番号順", "回転率①順", "回転率②順"]
)

if sort_option == "台番号順":
    df = df.sort_values('台番号')
elif sort_option == "回転率①順":
    df = df.sort_values('回転率①', ascending=False)
elif sort_option == "回転率②順":
    df = df.sort_values('回転率②', ascending=False)
```

#### 7. 台番号読み込み精度向上（問題10）
**改善内容:**
- OCRパラメータの調整
- 画像前処理の強化
- 正規化処理の改善

### 🟢 優先度低 - 後日対応検討

#### 8. エラー頻発問題（問題1,2）
**調査内容:**
- エラーログ追加
- メモリ使用量の監視
- 特定画像の問題分析

#### 9. 現在値と初当たり玉数の精度（問題11）
**改善内容:**
- グラフ解析アルゴリズムの調整
- ピーク検出の精度向上

#### 10. 大中小機能のエラー（問題12）
**対応:**
- 修正1,2完了後に再テスト
- エラー内容の詳細確認

## 実装スケジュール

### Phase 1（即座対応）
- [ ] 総獲得玉数の計算修正
- [ ] 回転率②の再計算
- [ ] 現在/通常回転数の表示修正
- [ ] セッションステート管理改善

### Phase 2（次回対応）
- [ ] ペアなし詳細画像の非表示
- [ ] 並び替え機能追加
- [ ] 台番号OCR精度向上

### Phase 3（検証後対応）
- [ ] エラー頻発の原因調査
- [ ] グラフ解析精度向上
- [ ] 大中小機能の動作確認

## テスト項目

1. 出玉詳細とグラフのペアリング動作
2. 総獲得玉数の計算精度
3. 回転率①②の計算精度
4. 複数回のアップロード時の動作
5. ファイル削除後の再アップロード
6. エクセル出力データの正確性

## 備考

- お客様のテスト結果を待って追加修正
- エラーログを収集して原因分析
- 段階的にリリースして動作確認

---
*最終更新: 2025年9月1日*